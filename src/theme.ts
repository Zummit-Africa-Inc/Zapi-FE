import { createTheme } from "@mui/material";

export const theme = createTheme({
    typography: {
        fontFamily: "var(--body-font)",
    },
    palette: {
        primary: {
            main: "#081F4A"
        },
        secondary: {
            main: "#FF5C00"
        },
    },
    components: {
    },
})